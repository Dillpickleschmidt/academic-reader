name: Build and Deploy Qwen3 TTS Worker

on:
  push:
    branches: [main]
    paths:
      - "workers/qwen3-tts/**"
      - ".github/workflows/deploy-worker-qwen3-tts.yml"

jobs:
  build-worker-qwen3-tts:
    runs-on: blacksmith-4vcpu-ubuntu-2404
    steps:
      - uses: actions/checkout@v4

      - name: Setup Blacksmith Builder
        uses: useblacksmith/setup-docker-builder@v1

      - uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - uses: useblacksmith/build-push-action@v2
        with:
          context: ./workers/qwen3-tts
          push: true
          tags: |
            dillpickleschmidt/academic-reader-worker-qwen3-tts:latest
            dillpickleschmidt/academic-reader-worker-qwen3-tts:${{ github.sha }}
          provenance: false
