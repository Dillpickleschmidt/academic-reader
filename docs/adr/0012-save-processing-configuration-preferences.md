# Save processing configuration preferences

Academic Reader saves Processing Configuration choices by default and reuses them for future Processing Runs. Narration is enabled on first run with a default voice, and later runs remember the reader's Narration, Conversion Model, OCR, page-range behavior, and related choices unless changed.
