# Blocks are the source of truth

Academic Reader stores Pages and Blocks as the primary representation of a Document; the Reader View is a projection. Blocks carry both a canonical Block Type and the original observed label from the conversion/OCR tool so Academic Reader can offer stable behavior without hiding provenance needed for inspection and debugging.
