# One processing run per source document

Academic Reader keeps exactly one Processing Run for each Source Document. If the reader wants a different conversion attempt, they create a new Source Document; this preserves a small code footprint and avoids version-management UI while still keeping the one run's Processing Events inspectable.
