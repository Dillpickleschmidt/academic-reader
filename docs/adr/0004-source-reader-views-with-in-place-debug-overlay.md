# Source View and Reader View are peers

Academic Reader presents the Source View and Reader View as peer views, side-by-side on larger screens and switchable on small screens. Debug information is shown in place with a Debug Overlay on the views themselves rather than through a separate inspector panel, because Academic Reader should make document structure visible without pulling the reader away from the Document.
